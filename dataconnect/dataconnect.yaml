specVersion: "v1"
serviceId: "civic"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "civic-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
